#spring.application.name=saloon-service
#
#server.port=8083
#
#
##mysql configuration
#
#spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=jdbc:mysql://localhost:3306/saloonDb
#spring.datasource.username=root
#spring.datasource.password=HA1256ri#
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true

spring:
  application:
    name: saloon-service

  datasource:
    url: jdbc:mysql://localhost:3306/saloonDb?createDatabaseIfNotExist=true
    username: root
    password: HA1256ri#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect  # Recommended for MySQL 8

server:
  port: 8083

eureka:
  instance:
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: "http://localhost:8088/eureka/"
    register-with-eureka: true
    fetch-registry: true
